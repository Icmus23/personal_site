{% extends 'menu.html.twig' %}

{% block body %}
    <h3>Admin Blog</h3>
    {% include 'admin/menu.html.twig' %}

    <h4>Edit Post</h4>
    {{ form_start(post_form) }}
    {{ form_widget(post_form) }}
    {{ form_end(post_form) }}
{% endblock %}
