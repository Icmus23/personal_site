{% extends 'menu.html.twig' %}

{% block body %}
    <h3>Admin Blog</h3>
    {% include 'admin/menu.html.twig' %}

    <h4>Add Post</h4>
    {{ form_start(add_post_form) }}
    {{ form_widget(add_post_form) }}
    {{ form_end(add_post_form) }}
{% endblock %}